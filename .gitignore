# Logs
logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
*.local

# env：常规环境文件（.env.development / .env.test / .env.production 等）进 git 跟踪
# 例外：.env.development.local 为开发环境本机私有覆盖文件，仅 development 模式读取，不进 git
.env.development.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# project data (WorkBuddy memory, not source)
.workbuddy/
